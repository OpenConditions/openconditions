# Prettier owns only what Biome cannot parse: Markdown, YAML and JSON5. Biome
# owns everything else (see biome.json), so the two never format one file.
node_modules/
dist/
build/
coverage/
.turbo/
pnpm-lock.yaml
.superpowers/
# drizzle-kit generated migrations + snapshots (never hand-edited)
**/drizzle/
# generated road-conditions-atlas seed (plain JSON in a .json5 file; regenerated by export:atlas)
packages/roads/atlas/roads.json5
# Python services own their own tooling
services/openlr-resolver/
# Generated by `pnpm gen:credentials` (scripts/gen-credentials.ts); the check-credentials
# pre-commit gate compares raw template output, so reformatting it would make the
# gate perpetually report drift.
docs/road-feed-credentials.md
