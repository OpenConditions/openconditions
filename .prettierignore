node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo
pnpm-lock.yaml
.superpowers/
# drizzle-kit generated migrations + snapshots (never hand-edited)
**/drizzle/
# generated road-conditions-atlas seed (plain JSON in a .json5 file; regenerated by export:atlas)
packages/roads/atlas/roads.json5
# Vendored TMC location table (regenerated by scripts/gen-tmc-table.ts). Held as
# one dense line; Prettier would expand it to hundreds of thousands of lines.
packages/roads/src/tmc/snapshots/
# Python services are not formatted by Prettier (they own their own tooling)
services/openlr-resolver/
# Generated by `pnpm gen:credentials` (scripts/gen-credentials.ts); the check-credentials
# pre-commit gate compares raw JSON.stringify()/template output, so Prettier reformatting
# these would make the gate perpetually report drift.
services/ingest/service.json
docs/road-feed-credentials.md
