{
  "name": "@openconditions/federation-api",
  "version": "0.1.0",
  "description": "OpenConditions federation service — instance actor document and peers discovery",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "tsx watch src/main.ts",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@openconditions/contributions-api": "workspace:*",
    "@openconditions/core": "workspace:*",
    "@openconditions/federation": "workspace:*",
    "@openconditions/ingest-framework": "workspace:*",
    "fastify": "^5.11.2",
    "postgres": "^3.4.9"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "testcontainers": "^12.0.4",
    "tsup": "^8.5.1",
    "tsx": "^4.23.5",
    "typescript": "^5.0.0",
    "vitest": "^4.1.10"
  }
}
